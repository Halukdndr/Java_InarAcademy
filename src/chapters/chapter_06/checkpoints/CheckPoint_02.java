package chapters.chapter_06.checkpoints;

public class CheckPoint_02 {
               //method definition
    // 1) public static => modifier
    // 2)  define type of value it's gonna return.
    // 3) define the method name
    // 4) then define the parameters
    // 5 finally build the method body
              //to invoke
    // 1) it depends on the method type(does return value or not) and how you are gonna use it .
    // 2) you must write method signiture and assing the parameters
    // 3) then you can print or assing another variable with it
}
