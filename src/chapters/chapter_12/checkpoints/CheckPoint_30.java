package chapters.chapter_12.checkpoints;

import java.io.PrintWriter;

public class CheckPoint_30 {

    /*
    PrintWriter output = new PrintWriter(File f)
    PrintWriter output = new PrintWriter(file name)
    because IOException ia a chected exception and JVM needs to be warned
    The file cannot be saved properly

     */
}
