package chapters.chapter_07.checkpoints;

public class CheckPoint_18 {
    public static void main(String[] args) {
        /*
        First main method takes its place in stack memory
        Then chars array created in the heap memory and its referance value added to stack memory
        displayArray method is invoked and added to stack memory
        Then created a new array named counts in the heap and also its refarence value in stack
        displayCounts is invoked and added to stack

         */
    }
}
