package chapters.chapter_07.checkpoints;

public class CheckPoint_05 {
    public static void main(String[] args) {
        /*
        int i = new int (30);  // invalid (use [] instead () )
        double d[] = new double[30]; // valid
        char[] r = new char(1..30); // invalid ([arraysize] not {content})
        int i[] = (3, 4, 3, 2); // invalid (needs curly brackets)
        float f[] = {2,3, 4,5, 6,6}; // invalid (cant contain double value)
        char[] c = new char(); // invalid (it needs [arraysize])
         */
    }
}
