package chapters.chapter_13.checkpoints;

public class CheckPoint_04 {
    public static void main(String[] args) {
        Number numberRef = new Integer(0);
        Double doubleRef = (double)numberRef;   // Number instance not a primitave data type because can't be casted by a primitive
    }
}
