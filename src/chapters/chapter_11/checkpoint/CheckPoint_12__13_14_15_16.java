package chapters.chapter_11.checkpoint;

public class CheckPoint_12__13_14_15_16 {
    /*12
     overriding methods have the same signiture and same returnd type in related two different classes
     overloading methods can be in same class or in related two different classes , they have same name but can't have same signiture .
     *13
     overridden
     *14
     Yes, it's a problem
     *15
     overloaded
     *16
     @Override annotation denotes that the annotated method is required to override a method int the superlcass.
     If a method with this annotation does not override its superclass's method,
     the compiler will report an error.

     */
}
